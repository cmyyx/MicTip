<Window x:Class="MicTip.UI.About.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关于 MicTip" SizeToContent="Height" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Microsoft YaHei UI"
        FontSize="13"
        Icon="/UI/Icons/mic-on.ico"
        ShowInTaskbar="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 图标 + 名称 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Image Source="/UI/Icons/mic-on.ico" Width="48" Height="48" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center" Margin="14,0,0,0">
                <TextBlock Text="MicTip" FontSize="22" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock x:Name="VersionText" Text="v0.1.0" FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Windows 麦克风静音工具 —— 全局快捷键静音麦克风，静音时显示悬浮窗提醒。"
                   Foreground="#374151" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- 仓库链接 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="项目地址: " Foreground="#374151" VerticalAlignment="Center"/>
            <TextBlock VerticalAlignment="Center">
                <Hyperlink x:Name="RepoLink" NavigateUri="https://github.com/cmyyx/MicTip"
                           RequestNavigate="OnRepoLinkClick" Foreground="#2563EB" TextDecorations="Underline">
                    github.com/cmyyx/MicTip
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <!-- 检查更新 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="CheckUpdateBtn" Content="检查更新" Width="96" Height="28" Margin="0,0,12,0" Click="OnCheckUpdate"/>
            <TextBlock x:Name="UpdateStatusText" Text="" VerticalAlignment="Center" Foreground="#6B7280"/>
        </StackPanel>

        <!-- 新版本提示 (默认隐藏) -->
        <Border x:Name="UpdatePanel" Grid.Row="4" CornerRadius="8" Padding="12,8" Margin="0,0,0,12"
                Background="#ECFDF5" BorderBrush="#A7F3D0" BorderThickness="1" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="●" Foreground="#10B981" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="UpdateDetailText" Text="" VerticalAlignment="Center" Foreground="#065F46"/>
            </StackPanel>
        </Border>

        <!-- 关闭 -->
        <Button Grid.Row="5" Content="关闭" Width="80" Height="30" HorizontalAlignment="Right" Click="OnClose"/>
    </Grid>
</Window>
